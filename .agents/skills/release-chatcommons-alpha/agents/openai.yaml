interface:
  display_name: "Release ChatCommons Alpha"
  short_description: "Prepare and deploy one verified friends-alpha release"
  default_prompt: "Use $release-chatcommons-alpha to prepare and deploy one verified friends-alpha release."
policy:
  allow_implicit_invocation: false
